<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml for MarkerDetectorApp
  
  Permissions explained:
  - CAMERA: Required for Vision Camera frame capture (core feature)
  - RECORD_AUDIO: Required for video recording with audio
  - INTERNET: Required for React Native dev server & any cloud processing
  - WRITE_EXTERNAL_STORAGE: For saving captured marker snapshots (Android ≤ 9)
  - READ_EXTERNAL_STORAGE: For loading reference marker templates (Android ≤ 9)
  - VIBRATE: For haptic feedback on detection events
  - WAKE_LOCK: Prevent screen sleep during continuous scanning sessions
  - FOREGROUND_SERVICE: For background scanning capability
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <!-- ==================== PERMISSIONS ==================== -->

  <!-- Camera — CRITICAL: Vision Camera will crash without this -->
  <uses-permission android:name="android.permission.CAMERA" />
  
  <!-- Camera feature declaration — allows Google Play filtering -->
  <uses-feature android:name="android.hardware.camera" android:required="true" />
  <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
  <uses-feature android:name="android.hardware.camera.flash" android:required="false" />

  <!-- Audio recording for video capture -->
  <uses-permission android:name="android.permission.RECORD_AUDIO" />

  <!-- Network for Metro bundler (debug) and optional cloud sync -->
  <uses-permission android:name="android.permission.INTERNET" />

  <!-- Storage — scoped storage preferred on Android 10+, legacy for older devices -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="29"
    tools:ignore="ScopedStorage" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />
  <!-- Android 13+ granular media permissions -->
  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
  <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

  <!-- Haptics -->
  <uses-permission android:name="android.permission.VIBRATE" />

  <!-- Screen wake lock for scanning sessions -->
  <uses-permission android:name="android.permission.WAKE_LOCK" />

  <!-- Foreground service for background processing -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />

  <!-- ==================== APPLICATION ==================== -->
  <application
    android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:allowBackup="false"
    android:theme="@style/AppTheme"
    android:hardwareAccelerated="true"
    android:supportsRtl="true"
    android:networkSecurityConfig="@xml/network_security_config"
    android:requestLegacyExternalStorage="true"
    tools:targetApi="31">

    <!-- Main Activity -->
    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:launchMode="singleTask"
      android:windowSoftInputMode="adjustResize"
      android:exported="true"
      android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- React Native Dev Settings (debug only) -->
    <activity
      android:name="com.facebook.react.devsupport.DevSettingsActivity"
      android:exported="false" />

    <!-- FileProvider for sharing captured images -->
    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_provider_paths" />
    </provider>

  </application>

</manifest>
